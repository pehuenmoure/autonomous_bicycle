<!DOCTYPE html>
<html>
<head>
	<script src="http://code.jquery.com/jquery-1.11.1.js"></script>
	<script src="/socket.io/socket.io.js"></script>

	<title>Index</title>

	 <style>
      * { margin: 0; padding: 0; box-sizing: border-box; }
      body { font: 13px Helvetica, Arial; }
      form { background: #000; padding: 3px; position: fixed; bottom: 0; width: 100%; }
      form input { border: 0; padding: 10px; width: 90%; margin-right: .5%; }
      form button { width: 9%; background: rgb(130, 224, 255); border: none; padding: 10px; }
      #data { list-style-type: none; margin: 0; padding: 0; }
      #data li { padding: 5px 10px; }
      #data li:nth-child(odd) { background: #eee; }
    </style>
</head>

<body>
	<h1>This is our data:</h1>
	<ul id = data></ul>

	<script type="text/javascript">

		var socket = io();

		socket.on('data-msg', function(d){
			console.log("data recieved - server side")
			$('#data').append('<li>' + separate(d) + '</li>');
			//test.append()
			/*x = $('#data').append('li').append('ul');
			for (i =0 ; i < d.length; i ++){
				y = x.append('li')
				y.text(d[i])
			}*/
		});

		function separate(array){
			var s = "";
			for (i = 0; i < array.length; i++){
				if (i+1 == array.length) {
					s = s + array[i];
				} else {
					s = s + array[i] + " , ";
				}
			}return s;
		}
	</script>
</body>

</html>